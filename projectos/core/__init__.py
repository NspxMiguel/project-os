"""Core subsystems: discovery, suggestions, plugins, store, sysinfo, services,
files, shell and the optional Home Assistant client.

Nothing here is imported eagerly by :mod:`projectos.core` itself -- import the
submodule you need so a missing optional dependency stays contained.
"""

from __future__ import annotations
